{
  "name": "immybox",
  "version": "1.0.0-beta3",
  "main": "build/immybox.js",
  "scripts": {
    "build:css": "chalk blue '=> compiling scss to css' && node-sass src/immybox.scss --output-style expanded > build/immybox.css && chalk green 'Done!'",
    "build:js": "chalk blue '=> transpiling ES6' && webpack && chalk green 'Done!'",
    "build": "npm run build:css -s && npm run build:js -s && cp src/immybox-arrow.png demo/immybox-arrow.png",
    "lint": "chalk blue '=> linting' && eslint src && chalk green 'All good!'",
    "watch": "chalk blue '=> building on changes to /src' && watch 'npm run lint -s && npm run build -s' src",
    "watch:lint": "chalk blue '=> linting on changes to /src' && watch 'npm run lint -s' src"
  },
  "devDependencies": {
    "babel-core": "6.x",
    "babel-loader": "6.x",
    "babel-plugin-ramda": "1.x",
    "babel-preset-es2015": "6.x",
    "chalk-cli": "3.x",
    "eslint": "3.x",
    "eslint-plugin-babel": "3.x",
    "node-sass": "3.x",
    "requirejs": "2.x",
    "rimraf": "^2.5.4",
    "uglify-js": "2.x",
    "unminified-webpack-plugin": "^1.1.0",
    "webpack": "2.1.0-beta.20"
  }
}
